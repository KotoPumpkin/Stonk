# Stonk - 股票模拟交易系统 全局开发准则

## 项目概述

本系统是一款基于 **C/S 架构** 的 Windows 桌面端股票模拟交易程序。系统支持多房间并行会话，允许管理员干预市场、管理机器人策略，并支持真人用户进行手动交易或量化代理交易。

---

## 一、角色与权限定义

### 1.1 管理员 (Server 端一体化)
- **形态**：服务器程序即管理员操作端，不设登录界面，拥有最高控制权
- **职责**：
  - 创建/销毁房间
  - 管理股票池和参数
  - 控制全局步进（秒/时/天/月）
  - 发布新闻和财报
  - 管控机器人账户和策略
  - 干预市场参数

### 1.2 真人账户 (Client 端)
- **形态**：独立客户端程序，需通过用户名/密码登录
- **安全**：密码采用 SHA-256 + Salt 加密存储
- **功能**：
  - 进入大厅查看房间列表
  - 加入/退出房间
  - 执行买卖操作
  - 查看盈亏和资产
  - 开启量化代理自动交易

### 1.3 机器人账户 (虚拟交易者)
- **形态**：服务器生成的虚拟交易者，归属于特定房间
- **策略类型**：
  1. **散户游资**：高频、追涨杀跌、受新闻情绪影响剧烈
  2. **正规机构**：价值导向、换手率低、对财报数据敏感
  3. **做空/做多组织**：趋势追踪、明显的单边操作倾向

---

## 二、房间与联机机制

### 2.1 房间大厅 (Lobby)
用户登录后进入大厅，可实时查看：
- **房间列表**：房间名称、当前步进模式（秒/时/天/月）、运行状态
- **人员构成**：在线真人用户 ID 列表、用户数量、机器人账户数量
- **房间状态**：运行中、暂停、快进中、已完成

### 2.2 房间隔离逻辑
每个房间是一个独立的"平行宇宙"，拥有：
- 独立的股票价格序列和历史行情
- 独立的时间戳（虚拟时间）
- 独立的交易订单池
- 独立的参与者集合（真人 + 机器人）
- 独立的资产快照

---

## 三、交易市场规则

### 3.1 核心交易规则
- **双向交易**：支持做多和做空，持仓可为负数（融券做空）
- **简化撮合**：
  - 不设涨跌停限制
  - 不设 ST 制度
  - 随时可按当前市价成交
  - 无滑点
- **资产构成**：
  - 现金（可为负，表示融资）
  - 股票持仓（可为负，表示融券）
  - 总资产 = 现金 + 所有股票价值
  - 盈亏 = 当前总资产 - 初始资金

### 3.2 步进模式 (Step Modes)

| 模式     | 步进单位 | 真人操作         | 图表类型      | 机器人行为       |
|---------|---------|-----------------|--------------|-----------------|
| 超短线   | 秒级    | 禁止手动，仅观看 | 折线图       | 高频决策（每秒）|
| 较短线   | 小时级  | 允许操作        | K线+指标    | 中频决策（每小时）|
| 短线     | 天级    | 允许操作        | K线+指标    | 低频决策（每天）|
| 中线     | 月级    | 允许操作        | K线+指标    | 按天决策（月内多次）|

---

## 四、步进控制流程

### 4.1 标准步进流程
1. **管理员触发**：点击"下一步"按钮
2. **进入决策期**：服务器广播信号，所有真人客户端进入"决策时间"（UI 锁定交互）
3. **协同确认**：
   - 所有真人需点击"完成"或执行买卖操作
   - 服务器等待收集全员就绪信号
4. **服务器计算**：
   - 生成新的价格数据
   - 执行机器人决策算法
   - 撮合所有挂单和成交
   - 更新所有参与者资产
5. **广播新状态**：发送更新的行情、资产、订单状态，解锁 UI

### 4.2 快进功能 (Fast Forward)
- 管理员开启快进后，服务器进入自动连续步进模式
- **强制托管**：快进期间真人禁止手动操作
- **量化代理**：真人可选开启自动交易代理，由服务器接管交易决策
- 快进可设定速度参数（每秒进行多少步）

---

## 五、动态干预系统

### 5.1 新闻发布系统
- **触发方式**：管理员随时主动发布
- **新闻属性**：
  - 标题（简短文本）
  - 内容（详细描述）
  - 情绪值（积极、消极、中立）
  - 影响范围（全局 or 特定股票）
  - 发布时间戳
- **实时效果**：
  - 即时广播至所有客户端弹出快讯
  - 即时修正机器人策略权重
  - 即时调整价格生成器的情绪参数

### 5.2 财报发布系统
- **周期探测**：服务器步进累计满 365 个天级单位时，自动提醒管理员
- **财报属性**：
  - 股票代码（该股独立发布）
  - 基本面指标（PE、ROE、净利润、营收等）
  - 管理员可编辑的对策略影响权重
- **长期影响**：影响该股机器人的持仓逻辑和估值模型

---

## 六、管理员管理模块

### 6.1 股票管理
- **基础参数**：
  - 股票代码和名称
  - 初始股价
  - 发行数量
  - 股票介绍（支持富文本）
- **干预权限**：
  - 可手动修正某支股票的价格走势参数
  - 可调整价格生成器的波动率、趋势

### 6.2 房间管理
- **创建房间**：
  - 指定房间名称
  - 设定初始步进模式
  - 配置参与股票池
  - 设置初始资金
- **销毁房间**：不可恢复，清空所有数据
- **人员管理**：查看实时在线人数、踢出用户

### 6.3 机器人管理 (房间内)
针对具体房间执行：
- 批量添加机器人，分配不同策略
- 修改机器人的初始资金规模
- 实时调整机器人的交易偏好参数
- 查看机器人持仓和操作历史

---

## 七、技术架构与选型

### 7.1 核心架构
- **架构模式**：C/S 架构 (Client/Server)
- **通信协议**：WebSocket + JSON（异步、全双工）
- **并发模型**：Python asyncio（异步协程）

### 7.2 前端技术栈
- **框架**：PySide6 (Qt for Python)
- **UI 风格**：扁平化设计、深色系金融风格
- **图表方案**：QtCharts（支持 K 线图、折线图）
- **指标支持**：MACD、KDJ、RSI 等技术指标叠加

### 7.3 后端技术栈
- **服务器**：Python asyncio WebSocket 服务器
- **数据库**：SQLite（存储用户、加密密码、历史行情、交易记录）
- **安全**：SHA-256 + Salt 密码加密

### 7.4 关键模块设计

#### 价格生成引擎 (price_engine.py)
- 支持多种价格模型（随机游走、均值回归、趋势跟踪等）
- 可动态调整波动率、漂移项
- 响应新闻情绪修正
- 支持每个房间独立生成

#### 策略引擎 (strategy_engine.py)
- 定义三类机器人的决策逻辑
- 响应财报数据修正权重
- 支持参数动态调整

#### 交易撮合引擎 (trade_manager.py)
- 处理买卖挂单、撮合成交
- 维护订单簿 (Order Book)
- 计算成交价格、费用

#### 数据存储 (models.py)
- 用户表、房间表、股票表、机器人表
- 交易记录表、资产快照表

---

## 八、界面设计规范

### 8.1 管理员端（Server UI）
- **布局**：多标签页结构，每个标签管理一个房间
- **标签页内容**：
  - **房间控制**：步进按钮、快进开关、暂停/恢复
  - **参与者列表**：真人用户、机器人列表（含策略和资金）
  - **股票管理**：当前价格、交易量、参数编辑
  - **新闻发布器**：快讯编辑和发送
  - **干预工具**：价格干预、机器人参数调整

### 8.2 客户端（Client UI）

#### 登录界面
- 用户名、密码输入框
- 登录/注册按钮

#### 大厅界面
- **左侧**：房间列表（可排序、过滤）
- **中央**：房间详情卡片（人数、状态、步进模式）
- **按钮**：加入房间、创建房间（若开放）

#### 交易界面（房间内）
- **左侧/中部**：K 线或折线图（根据步进模式选择）
  - 图表顶部：股票选择下拉框
  - 指标叠加：MACD、KDJ 等
  - X 轴：时间戳
  - Y 轴：价格
- **右侧**：交易下单区
  - 输入框：股票代码、交易数量
  - 按钮：买入、卖出、取消订单
  - 确认按钮：完成（等待步进）
- **底部**：
  - 持仓列表（股票代码、数量、成本价、当前价、浮盈）
  - 资产概览（总资产、现金、盈亏、盈亏比）
  - 快讯弹窗（显示管理员发布的新闻）

### 8.3 设计原则
- **扁平化**：无渐变、阴影最小化
- **配色**：深色底 (#1a1a1a) + 金融绿/红 (#00ff00 / #ff0000)
- **间距**：统一 12px 基础单位
- **字体**：Microsoft YaHei 或 Segoe UI，主体 12px，标题 16px+

---

## 九、开发规范

### 9.1 代码组织
```
Stonk/
├── server/
│   ├── __init__.py
│   ├── websocket_server.py    # WebSocket 服务器主程序
│   ├── price_engine.py         # 价格生成引擎
│   ├── strategy_engine.py      # 机器人策略引擎
│   ├── trade_manager.py        # 交易撮合引擎
│   ├── models.py               # 数据模型和数据库操作
│   ├── admin_tools.py          # 管理员工具和干预接口
│   ├── admin_ui.py             # 管理员 UI (PySide6)
│   └── config.py               # 配置文件
├── client/
│   ├── __init__.py
│   ├── websocket_client.py     # WebSocket 客户端
│   ├── ui/
│   │   ├── __init__.py
│   │   ├── main_window.py      # 主窗口
│   │   ├── login_window.py     # 登录窗口
│   │   ├── lobby_window.py     # 大厅窗口
│   │   ├── trading_window.py   # 交易窗口
│   │   └── widgets.py          # 自定义控件
│   └── config.py               # 客户端配置
├── shared/
│   ├── __init__.py
│   ├── constants.py            # 常量定义
│   ├── message_protocol.py     # 消息协议定义
│   └── utils.py                # 共享工具函数
├── .clineRules                 # 本文件（全局开发准则）
├── MEMORY.md                   # 开发进度跟踪
├── requirements.txt            # 依赖列表
└── README.md                   # 项目说明
```

### 9.2 命名规范
- **Python 模块**：snake_case (trade_manager.py)
- **Python 类**：PascalCase (TradeManager)
- **Python 函数/变量**：snake_case (handle_trade_order)
- **常量**：UPPER_SNAKE_CASE (MAX_ROOMS)
- **WebSocket 消息类型**：UPPER_SNAKE_CASE (PRICE_UPDATE)

### 9.3 消息协议规范
所有 WebSocket 消息采用统一 JSON 格式：
```json
{
  "type": "MESSAGE_TYPE",
  "data": {
    "key": "value"
  },
  "timestamp": 1234567890,
  "room_id": "optional"
}
```

### 9.4 数据库设计
- 使用 SQLite，支持事务
- 所有时间戳采用 Unix Timestamp 或 ISO 8601
- 密码存储：SHA-256(password + salt)
- 定期备份关键表（交易记录）

### 9.5 错误处理
- 所有网络异常均应捕获并返回友好错误信息
- 数据库操作应使用事务确保一致性
- 客户端应有重连机制（指数退避）

### 9.6 测试规范
- 编写单元测试覆盖核心引擎（价格、策略、撮合）
- 编写集成测试覆盖 WebSocket 通信流程
- 编写 UI 测试覆盖关键交互路径

### 9.7 Shell 命令规范
所有 shell 命令应采用标准 PowerShell 兼容格式：
- **使用 PowerShell 语法**：所有命令行脚本优先采用 PowerShell 兼容的写法
- **跨平台兼容性**：优先使用 PowerShell Core 的跨平台命令
- **命令示例**：
  - `python` 或 `python.exe`（不使用 `py`）
  - `Get-ChildItem` 而非 `ls`
  - `Remove-Item` 而非 `rm`
  - `Set-Location` 而非 `cd`（如必须改变目录，使用 `cd` 后跟 `&&` 连接后续命令）
- **脚本扩展名**：推荐使用 `.ps1` 后缀的 PowerShell 脚本
- **环境变量**：使用 `$env:VARIABLE_NAME` 访问环境变量（而非 `$VARIABLE_NAME`）

---

## 十、开发优先级和依赖

### Phase 1：基础架构 (必须)
1. 数据模型定义 (models.py)
2. WebSocket 通信框架 (websocket_server.py + websocket_client.py)
3. 用户认证与会话管理
4. 基础 UI 框架 (登录、大厅)

### Phase 2：核心交易逻辑 (必须)
1. 价格生成引擎 (price_engine.py)
2. 交易撮合引擎 (trade_manager.py)
3. 步进控制逻辑
4. 交易 UI (图表、下单)

### Phase 3：机器人与策略 (重要)
1. 策略引擎 (strategy_engine.py)
2. 三类机器人的具体实现
3. 机器人管理 UI

### Phase 4：管理员干预 (重要)
1. 新闻发布系统
2. 财报系统
3. 管理员 UI
4. 价格/参数干预工具

### Phase 5：优化与打磨 (可选)
1. 性能优化
2. 图表美化与指标支持
3. 数据导出/分析功能
4. 完整测试覆盖

---

## 十一、版本控制与协作

### 11.1 Git 工作流
- **主分支 (main)**：稳定发行版本
- **开发分支 (develop)**：集成分支
- **特性分支 (feature/\*)**：每个功能一个分支
- **修复分支 (bugfix/\*)**：每个 bug 一个分支

### 11.2 提交规范
```
[Phase] [模块] 简短描述

详细说明（可选）

关联 Issue: #123
```

例如：
```
[Phase 1] [Auth] 实现用户登录认证

- 添加 SHA-256+Salt 密码加密
- 实现会话管理
- 添加 token 刷新机制

关联 Issue: #1
```

---

## 十二、核心约定与最佳实践

1. **原子性提交**：每次提交应代表一个完整的功能单元
2. **代码审查**：所有 PR 需要代码审查后才能合并
3. **文档同步**：任何 API 变更需同步更新 MEMORY.md
4. **向后兼容**：尽可能保持消息协议的向后兼容性
5. **安全第一**：密码不得明文存储，所有网络通信应加密
6. **用户体验**：客户端应有清晰的加载状态和错误提示
7. **可扩展性**：代码应易于添加新的策略类型、房间模式、图表指标

---

## 十三、参考资源与链接

- PySide6 文档：https://doc.qt.io/qtforpython/
- Python asyncio：https://docs.python.org/3/library/asyncio.html
- WebSocket 协议：https://tools.ietf.org/html/rfc6455
- SQLite 文档：https://www.sqlite.org/docs.html

---

**最后更新**：2026-03-11
**版本**：1.0
